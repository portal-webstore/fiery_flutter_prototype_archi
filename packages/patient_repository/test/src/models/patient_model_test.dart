import 'package:flutter_test/flutter_test.dart';

void main() {
  group('patient model', () {
    test('parses map', () {
      expect(true, true);
    });
    test('parses json', () {
      expect(true, true);
    });
  });
}
