import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Clinic repository', () {
    test('parses static json', () {
      expect(true, true);
    });
    test('parses product preferences', () {
      expect(true, true);
    });
  });
}
