name: authentication_repository
description: Repository for mediating logins
version: 0.0.1
author: "sl"
homepage: "https://github.com/portal-webstore/fiery_flutter_prototype_archi"
publish_to: none

environment:
  sdk: ">=2.13.0 <3.0.0"
  flutter: ">=2.2.0"

dependencies:
  flutter:
    sdk: flutter
  cache_client:
    path: ../cache_client
  cloud_firestore: ^2.0.0
  equatable: ^2.0.0
  firebase_auth: ^1.1.4
  shared_preferences: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
