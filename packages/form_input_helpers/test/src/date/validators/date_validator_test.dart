import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Validate date', () {
    test('disallows invalid date text', () {
      expect(true, true);
    });
    test('disallows invalid date range', () {
      expect(true, true);
    });
  });
}
