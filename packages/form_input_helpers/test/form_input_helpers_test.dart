import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Form input helpers', () {
    test('disallows invalid date', () {
      expect(true, true);
    });
    testWidgets(
      'prevents some invalid decimal text',
      (
        WidgetTester tester,
      ) async {
        expect(true, true);
      },
    );
    test('disallows empty ', () {
      expect(true, true);
    });
  });
}
