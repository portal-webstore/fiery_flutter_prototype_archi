import 'package:flutter_test/flutter_test.dart';

void main() {
  group('date parser formats', () {
    test('parses date', () {
      expect(true, true);
    });
    test('formats date', () {
      expect(true, true);
    });
  });
}
