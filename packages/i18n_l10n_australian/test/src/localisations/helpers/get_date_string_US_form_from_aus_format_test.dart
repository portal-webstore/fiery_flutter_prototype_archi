import 'package:flutter_test/flutter_test.dart';

void main() {
  group('converts Australian to United States date format', () {
    test('dd/MM/yyyy to MM/dd/yyyy', () {
      expect(true, true);
    });
  });
}
