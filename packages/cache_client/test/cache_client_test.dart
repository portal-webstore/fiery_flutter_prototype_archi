import 'package:flutter_test/flutter_test.dart';

void main() {
  group('caches values', () {
    test('read/write key', () {
      expect(true, true);
    });
    test('web memory limit', () {
      /// Find the load test point
      expect(true, true);
    });
  });
}
